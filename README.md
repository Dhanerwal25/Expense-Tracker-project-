# Expense-Tracker-project-
A command-line tool built to manage daily expenses using Python’s core data structures.This project focuses on efficient data handling using Python's built-in collection types without needing external heavy libraries."
